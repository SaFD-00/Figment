import * as React from "react";
import { FeatureGrid } from "figment-frontend";

// Static three-pillar showcase (Generate / Edit / Vectorize). No props.
export const Default = () => (
  <div style={{ maxWidth: 980 }}>
    <FeatureGrid />
  </div>
);
